- add man page
- add restart settings(intensity and period settings - if we exceed this, sv exits)
- use getopt.sh for option parsing instead of environment variables?
- -d option for debug mode? do not daemonize and do not redirect stderr and stdout.
- split start event into pre-start and post-start?
- split sopt event into pre-stop and post-stop?
